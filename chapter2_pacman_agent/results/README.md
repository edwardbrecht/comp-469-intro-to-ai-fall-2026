Trial and training output lands here.

`python tools/run_trials.py` writes `trials.csv` (one row per episode) and
`summary.csv` (one row per agent). `python tools/train_learning_agent.py`
writes `learned_weights.json` (Part 6's starting and best weights, and its
before/after mean performance). All three are part of your submission.
